$schema: https://learning-contents.org/schemas/free-coding/0.1.0
source: main.ts
license: MIT
locale: en-US
id: free_coding_bank_account
title: Bank Account Management
description: Implement and test bank account methods in TypeScript.
authors:
  - first: Jone
    last: Doe
    email: jone.doe@example.com
    affiliation: University of John Doe
pedagogy:
  activity: free-coding
  difficulty: novice
  prereq_topics: [classes, types]
  topics: [classes, methods, state, types]
runtime:
  language: typescript
statement: |
  Implement a `transfer` sequence or method in `main` to move money from one `BankAccount` to another.
  Ensure that money is withdrawn from the current account only if funds are sufficient, and deposited into the target account.
executions:
  - title: Transfer Alice to Bob
    stdin: []
    stdout: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    console: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    status: pass
elements:
  - type: locked
    location:
      snippet: |-
        interface IBankAccount {
            owner: string;
            balance: number;
            deposit(amount: number): number;
            withdraw(amount: number): number;
        }

        class BankAccount implements IBankAccount {
            owner: string;
            balance: number;

            constructor(owner: string, balance: number) {
                this.owner = owner;
                this.balance = balance;
            }

            deposit(amount: number): number {
                if (amount > 0) {
                    this.balance = this.balance + amount;
                    console.log(`Deposited ${amount}`);
                }
                return this.balance;
            }

            withdraw(amount: number): number {
                if (0 < amount && amount <= this.balance) {
                    this.balance = this.balance - amount;
                    console.log(`Withdrew ${amount}`);
                } else {
                    console.log("Insufficient funds");
                }
                return this.balance;
            }
        }
  - type: editable
    location:
      snippet: |-
        const account: BankAccount = new BankAccount("Bob", 200);
            account.deposit(100);
            account.withdraw(50);
            console.log(`Balance: ${account.balance}`);
    placeholder: |-
      // Implement transfer sequence here
          // Then test it with acc1 and acc2
testcases:
  - title: Simple Transfer
    visible: true
    stdin: []
    stdout: |-
      Deposited 100
      Withdrew 50
      Balance: 250
    console: |-
      Deposited 100
      Withdrew 50
      Balance: 250
